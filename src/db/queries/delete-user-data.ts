// import { getDB } from '..';

// NOTE: This function is triggered by an inbound Clerk webhook whenever a user account is deleted.
// NOTE: Use it to clean up any stray user data.

export const deleteUserData = async () => {
  // const db = getDB();
  // await db.transaction(async (transaction) => {});
}
